type Point is ({int x, int y} this)

assert "assertion failed":
    forall(&Point p, &Point q):
        if:
            p == q
        then:
            (*p).x == (*q).x
            (*p).y == (*q).y

