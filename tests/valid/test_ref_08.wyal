type Point is ({int x, int y} self)

assert "assertion failed":
    forall(&Point p, &Point q):
        if:
            p == q
        then:
            (*p).x == (*q).x
            (*p).y == (*q).y

