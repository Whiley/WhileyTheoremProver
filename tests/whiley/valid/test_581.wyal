type SizeGetter is (any self)


type SizeSetter is (any self)


assert "type invariant not satisfied":
    forall(any $0):
        if:
            true
        then:
            $0 is SizeGetter|SizeSetter


