type nat is (int x)
where:
    x >= 0


assert "index out of bounds (not less than length)":
    forall(int[] input, nat pos):
        if:
            pos < |input|
        then:
            pos < |input|


