type nat is (int n)
where:
    n >= 0


type expr is ({expr left, int op, expr right}|nat self)


assert "assertion failed":
    forall(expr e):
        if:
            e == 14897
        then:
            e == 14897


