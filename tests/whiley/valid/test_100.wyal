assert "index out of bounds (not less than length)":
    forall(bool[] board, int i):
        if:
            i > 0
            (i + 1) < |board|
            board[i - 1]
        then:
            (i + 1) < |board|


