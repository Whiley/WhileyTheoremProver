function buildNatList(int n) -> (int[] m)

define buildNatList_requires_0(int n) is:
    n >= 0


assert "precondition not satisfied":
    if:
        buildNatList(0) == [0;0]
        buildNatList(1) == [0]
        buildNatList(2) == [0, 1]
        buildNatList(3) == [0, 1, 2]
        buildNatList(4) == [0, 1, 2, 3]
    then:
        buildNatList_requires_0(10)


