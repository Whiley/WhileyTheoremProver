assert "index out of bounds (not less than length)":
    forall(int i, int[] arr2, int[] arr1):
        if:
            arr1 == [1, 2, 64]
            arr2 == arr1
            i != |arr1|
        then:
            2 < |arr2|


