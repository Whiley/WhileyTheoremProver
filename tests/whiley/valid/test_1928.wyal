define f_loopinvariant_19(int[] xs, int i) is:
    i >= 0


assert "loop invariant does not hold on entry":
    forall(int i, int[] xs):
        if:
            i == 0
        then:
            f_loopinvariant_19(xs, i)


