assert "index out of bounds (not less than length)":
    forall(int[] arr):
        if:
            arr == [1, 2, 3]
            arr[0] < |arr|
            arr[1] < |arr|
        then:
            2 < |arr|


