type SyntaxError is ({int[] msg} self)


assert "type invariant not satisfied":
    forall(int[] errorMessage):
        if:
            true
        then:
            {msg: errorMessage} is SyntaxError


