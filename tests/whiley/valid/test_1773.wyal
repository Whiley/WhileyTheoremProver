define find_loopinvariant_31(int[] items, int i) is:
    (0 <= i) && (i <= |items|)


assert "loop invariant does not hold on entry":
    forall(int i, int[] items):
        if:
            i == 0
        then:
            find_loopinvariant_31(items, i)


