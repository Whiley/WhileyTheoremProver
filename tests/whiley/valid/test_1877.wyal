define buildNatList_requires_0(int n) is:
    n >= 0


assert "negative length possible":
    forall(int i, int n):
        if:
            buildNatList_requires_0(n)
            i == 0
        then:
            n >= 0


