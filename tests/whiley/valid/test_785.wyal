assert "index out of bounds (not less than length)":
    forall(int[] arr2$1, int[] arr2, int[] arr1):
        if:
            arr1 == [1, 2, 3]
            arr2 == arr1
            arr2$1 == arr2[2:=2]
        then:
            2 < |arr2$1|


