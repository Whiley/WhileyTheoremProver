type intr is (bool|int self)


assert "index out of bounds (not less than length)":
    forall(((int&intr)[])[] e$1, (intr[])[] e):
        if:
            e$1 == e
            (e is (int[])[]) && (|e$1| > 0)
        then:
            0 < |e$1|


